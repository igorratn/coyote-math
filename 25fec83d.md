Let $P_n^{(\alpha,\beta)}(x)$ denote the Jacobi polynomials, orthogonal on $[-1,1]$ with respect to $p(x)=(1-x)^\alpha(1+x)^\beta$. Let $Q_n^{(\alpha,\beta)}(z)$ be the corresponding functions of the second kind defined for $z \in \mathbb{C} \setminus [-1,1]$ by:

$$Q_n^{(\alpha,\beta)}(z) = \frac{1}{2} \int_{-1}^1 \frac{P_n^{(\alpha,\beta)}(t)(1-t)^\alpha(1+t)^\beta}{z-t} dt$$

Consider the specific case $\alpha = \beta = 0$ (Legendre case), so $p(x)=1$. Define the sequence of functions:

$$f_n(z) = Q_n(z) P_{n+1}(z) - Q_{n+1}(z) P_n(z)$$

Claim: For all $n \ge 0$, the function $f_n(z)$ is a polynomial in $z$ of degree exactly $0$.

Determine whether this claim is True or False, and provide a rigorous proof.


conclusion: true

proof.

for $z \in \mathbb{C} \setminus [-1, 1]$ we use the definition

$$Q_n(z) = \frac{1}{2} \int_{-1}^1 \frac{P_n(t)}{z - t} dt$$

to write

$$f_n(z) = Q_n(z) P_{n+1}(z) - Q_{n+1}(z) P_n(z)$$

$$f_n(z) = \frac{1}{2} \int_{-1}^1 \frac{P_n(t) P_{n+1}(z) - P_{n+1}(t) P_n(z)}{z - t} dt.$$

the numerator vanishes at $t = z$, hence $(z - t)$ divides it and the quotient

$$K_n(z, t) = \frac{P_{n+1}(z) P_n(t) - P_n(z) P_{n+1}(t)}{z - t}$$

is a polynomial in $z$ and $t$. by the christoffel-darboux formula for legendre polynomials,

$$K_n(z, t) = \sum_{k=0}^n \frac{2k + 1}{2} P_k(z) P_k(t).$$

therefore

$$f_n(z) = \frac{1}{2} \int_{-1}^1 K_n(z, t) dt$$

$$f_n(z) = \frac{1}{2} \sum_{k=0}^n \frac{2k + 1}{2} P_k(z) \int_{-1}^1 P_k(t) dt.$$

now $\int_{-1}^1 P_0(t) dt = 2$ and $\int_{-1}^1 P_k(t) dt = 0$ for $k \ge 1$ by orthogonality against $P_0 \equiv 1$. hence only the $k = 0$ term remains:

$$f_n(z) = \frac{1}{2} \cdot \frac{1}{2} \cdot P_0(z) \cdot 2 = \frac{1}{2}.$$

thus $f_n(z)$ is a polynomial of degree 0 (a constant), independent of $z$, for every $n \ge 0$.

final answer: true.