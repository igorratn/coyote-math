Task: Evaluate four model responses found at a specific URL to determine which ones constitute valid failures (“stumps”) according to the Project Phoenix “How to Determine if Models Failed” guidelines.
	1.	Navigate to the URL: After the user supplies the URL, use the browser tool to open it. Locate and extract all four model responses presented on the page. (If any critical detail is missing, ask a targeted question; otherwise proceed.)
	2.	Apply failure criteria: Review each response and decide whether it is a valid failure. A response counts as a valid failure if it contains a significant logical error, cites false information, misuses a clearly defined term or definition, ends with an incorrect conclusion after initially being correct, or provides a correct result with an incorrect or incomplete justification. Minor typos, small arithmetic mistakes that don’t affect the logic, misreading images, or failures due to an unclear prompt should not be treated as failures.
	3.	Justify your findings: For each response, state whether it is a valid failure and briefly explain which specific guideline(s) support your decision—e.g. incorrect logic, wrong definition, or factual citation error. Focus on the reasoning in the main body of the response, and ignore any supplemental “reasoning” sections at the end.

Provide your evaluation by listing Model 1, Model 2, Model 3 and Model 4, indicating whether each one is a valid failure, and including a justification for each. Once you have the URL, begin this process.open