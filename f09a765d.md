For $x > 0$, define the Kelvin functions of order zero by

$$J_0\!\bigl(x\,e^{i\pi/4}\bigr) = \operatorname{ber}(x) + i\,\operatorname{bei}(x), \qquad K_0\!\bigl(x\,e^{i\pi/4}\bigr) = \operatorname{ker}(x) + i\,\operatorname{kei}(x),$$

where $J_0$ is the Bessel function of the first kind and $K_0$ is the modified Bessel function of the second kind, both of order zero.

Define

$$W_1(x) = \operatorname{ber}(x)\,\operatorname{ker}'(x) - \operatorname{ber}'(x)\,\operatorname{ker}(x), \qquad W_2(x) = \operatorname{bei}(x)\,\operatorname{kei}'(x) - \operatorname{bei}'(x)\,\operatorname{kei}(x).$$

Claim: For all $x > 0$,

$$W_1(x) - W_2(x) = \frac{1}{x}.$$

Determine whether this claim is True or False, and give a rigorous proof of your conclusion.

You're right — my solution used the wrong connection formula. The asymptotic approach is cleaner and fully rigorous. Here's the corrected solution:

**The claim is False.**

Set $z = xe^{i\pi/4}$. From the standard expansions near $0$,

$$J_0(z) = 1 - \frac{z^2}{4} + O(z^4), \qquad K_0(z) = -\log\frac{z}{2} - \gamma + O(z^2 \log z).$$

Since $z^2 = ix^2$ and $\log z = \log x + i\pi/4$, this gives

$$\operatorname{ber}(x) = 1 + O(x^4), \qquad \operatorname{bei}(x) = -\frac{x^2}{4} + O(x^4),$$

$$\operatorname{ker}(x) = -\log\frac{x}{2} - \gamma + O(x^2 \log x), \qquad \operatorname{kei}(x) = -\frac{\pi}{4} + O(x^2 \log x).$$

Hence

$$\operatorname{ber}'(x) = O(x^3), \qquad \operatorname{bei}'(x) = -\frac{x}{2} + O(x^3),$$

$$\operatorname{ker}'(x) = -\frac{1}{x} + O(x \log x), \qquad \operatorname{kei}'(x) = O(x \log x).$$

Therefore

$$W_1(x) = \operatorname{ber}(x)\,\operatorname{ker}'(x) - \operatorname{ber}'(x)\,\operatorname{ker}(x) = -\frac{1}{x} + O(x \log x),$$

$$W_2(x) = \operatorname{bei}(x)\,\operatorname{kei}'(x) - \operatorname{bei}'(x)\,\operatorname{kei}(x) = O(x).$$

So

$$W_1(x) - W_2(x) = -\frac{1}{x} + O(x \log x) \qquad (x \to 0^+).$$

In particular, it cannot equal $+1/x$ for all $x > 0$. The claim is **False**.